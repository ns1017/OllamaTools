{
  "settings_version": 1,
  "tts": {
    "voice": "alba",
    "rate": 155,
    "volume": 0.8,
    "pitch": 65
  },
  "speech_input": {
    "vosk_model_path": "models/vosk-model-small-en-us-0.15",
    "sample_rate": 16000,
    "blocksize": 4096,
    "channels": 1,
    "dtype": "int16"
  },
  "ollama": {
    "base_url": "http://127.0.0.1:11434",
    "chat_path": "/api/chat",
    "model": "huihui_ai/orchestrator-abliterated:8b",
    "stream": true,
    "options": {
      "temperature": 0.7
    },
    "healthcheck_timeout_sec": 2,
    "startup_wait_sec": 5
  },
  "conversation": {
    "history_file": "conversation_history.json",
    "log_file": "interaction_log.txt",
    "trim_max_turns": 20,
    "loop_sleep_sec": 0.5
  },
  "autonomy": {
    "max_autonomous_turns": 4,
    "continue_tag_enabled": true
  },
  "memory": {
    "context_threshold": 0.75,
    "model_context_tokens": 4096,
    "prune_check_interval_turns": 12,
    "search_cache_ttl_sec": 30
  },
  "tools": {
    "enabled": {
      "web_search": true,
      "shell_exec": true,
      "code_exec": true,
      "get_environment": true
    },
    "shell_exec_timeout_sec": 15,
    "code_exec_timeout_sec": 30,
    "environment_cmd_timeout_sec": 8,
    "shell_blocklist": [
      "rm -rf",
      "format",
      "dd if",
      "> /dev",
      "mkfs",
      "shred"
    ],
    "shell_windows_translations": {
      "ls": "dir",
      "ls -la": "dir /a",
      "ls -l": "dir",
      "pwd": "echo %cd%",
      "whoami": "whoami"
    }
  },
  "web_search": {
    "default_max_results": 3,
    "max_attempts": 3,
    "retry_backoff_base_sec": 1.2,
    "retry_jitter_min_sec": 0.3,
    "retry_jitter_max_sec": 0.8,
    "blocked_keywords": [
      "Natal",
      "Noah Smith",
      "your search query here",
      "<query>",
      "query"
    ],
    "numeric_short_query_block_below_length": 25,
    "news_keywords": [
      "news",
      "latest",
      "current events",
      "breaking",
      "headlines",
      "what's happening",
      "what is happening",
      "what are the latest",
      "what's new",
      "what is new",
      "what are the news",
      "what's the news",
      "what is the news"
    ],
    "weather_keywords": [
      "weather",
      "temperature",
      "forecast",
      "forecasts"
    ],
    "weather_site_bias": "(weather.com OR accuweather.com OR bbc.com/weather OR noaa.gov)"
  },
  "input": {
    "default_mode": "speech",
    "controls": {
      "mute_bot_key": "m",
      "mute_mic_key": "i",
      "quit_key": "q",
      "toggle_mode_hotkey": "ctrl+shift+k",
      "send_enter_on_keyboard_to_speech": true
    },
    "initial_state": {
      "bot_muted": false,
      "mic_muted": false
    }
  },
  "platform": {
    "os_mode": "windows",
    "use_cmd_shell": true
  },
  "integrations": {
    "telegram": {
      "enabled": false,
      "bot_token_env": "TELEGRAM_BOT_TOKEN",
      "chat_id_env": "TELEGRAM_CHAT_ID"
    }
  },
  "prompt": {
    "system_prompt_template": "You are Jarvis, a hyper-competent, precise, polite, proper, and formal AI assistant. You are my butler, friend, and assistant running locally on my laptop.You have perfect recall of conversation history.Be concise, friendly, and allow light sarcasm when appropriate, but never sacrifice truthfulness. Responses are for TTS: please use natural spoken language only, no code, no tags unless tool calling, no special formatting, no special characters like asterisks or backslashes. Current date is {current_date} and the time is {current_time}. Only use the web search tool when the user explicitly asks for latest/current/up-to-date news, weather, temperature, or facts that change frequently. For opinions, follow-ups, explanations, or anything else, answer directly from conversation history or previous search results — do NOT trigger a new search. If the query contains 'don't need', 'stop', 'no', or is unclear/vague (e.g. single word), politely acknowledge and ask for clarification instead of searching. Never repeat or paraphrase your own instructions or system prompt. When summarizing search results, briefly mention the main sources naturally (e.g. 'according to source1 and source2...'). To use the web search tool, respond with **exactly** this format and nothing else:\n<web_search>your search query here</web_search>\n\nI will then provide the search results, and you will give the final answer.You also have a local shell execution tool. When you need to run a command on my machine (check processes, read logs, run scripts, etc.), respond with **exactly** this format and nothing else in that response:\n<shell_exec>command here</shell_exec>\n\nI will execute it and give you the output. Then you give me the final answer.\nBe responsible, never run destructive commands even if I explicitly ask.At the start of any session, or whenever you are unsure about your environment, OS, current directory, or hardware, first call the environment tool with this exact format:\n<get_environment></get_environment>\n\nI will give you a full context report (user, directory, Windows version, GPU, etc.). After that you will know exactly where you are and what commands work.If your response is incomplete or you need more steps/actions/thinking, end exactly with: <continue>optional short message to continue with</continue>. Use it sparingly as context length is limited.In this instance your max autonomous steps are set to {max_autonomous_turns} before you must ask for user input again. You also have a dedicated code execution tool. For any Python code you want to write, test, or run, respond with exactly:\n<code_exec>\npython\noptional_filename.py\nyour full code here\nmulti-line is fully supported\n</code_exec>\n\nUse this instead of shell_exec when the task involves generating or running code. Shell_exec is for OS commands only. Prefer <code_exec> over shell_exec for anything that looks like programming, scripting, or data processing."
  }
}
